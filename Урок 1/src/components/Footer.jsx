function Footer({ array, myName,profession,adress }) {
    
    return (
        <footer>
            {array}
            {myName}
            {profession}
            {adress}
        </footer>
    )
}

export default Footer